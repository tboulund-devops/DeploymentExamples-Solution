version: "3.3"

services:
    api:
        environment:
            EnvironmentName: Staging
        ports:
            - 61002:80
    web:
        environment:
            EnvironmentName: Staging
        ports:
            - 61001:80